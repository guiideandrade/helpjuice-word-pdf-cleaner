{
  "name": "helpjuice-word-pdf-cleaner",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Client-side Word/PDF HTML cleaner for Helpjuice — transform + validate + report pipeline.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npm run build && npx --yes gh-pages@6 -d dist --dotfiles -m \"chore: deploy $(node -p \"require('./package.json').version\")\""
  },
  "dependencies": {
    "dompurify": "^3.2.4"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.1.0",
    "vitest": "^2.1.8"
  }
}
